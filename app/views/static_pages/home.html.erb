<% provide(:title, '') %>
<div class="center hero-unit">
	<h1>Media Manager</h1>
	<h2>This is the home page for the Media Manager</h2>
	
	<% if signed_in? %>
		<%= link_to 'Movies', movies_path, class: "btn btn-large btn-primary" %>
	<% else %>
		<%= link_to 'Sign in', signin_path, class: "btn btn-large btn-primary" %>
	<% end %>
</div>