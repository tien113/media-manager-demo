<%= render 'shared/error_messages', object: f.object %>
		
<%= f.text_field :title, { placeholder: "Title" } %>

<%= f.text_field :original_title, { placeholder: "Original Title" } %>

<%= f.text_field :year, { placeholder: "Year" } %>

<%= f.text_field :rating, { placeholder: "Rating" } %>

<%= f.text_field :genre, { placeholder: "Genre" } %>

<%= f.text_field :imdb_url, { placeholder: "IMDb URL" } %>

<%= f.text_field :image_url, { placeholder: "Image URL" } %>

<%= f.text_field :storage, { placeholder: "Storage" } %>

<%= f.select(:resolution, resolutions, { include_blank: true, prompt: "Select Resolution" }, { class: "span3" }) %>

<%= f.label :watched, class: "checkbox" do %>
	<%= f.check_box :watched %> Watched
<% end %>

